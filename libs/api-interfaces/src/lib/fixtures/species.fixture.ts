import { SpeciesApiResponse } from '@pokedex/api-interfaces';

export const species = [
  { id: 1, name: 'bulbusaur', evolution_chain: { url: 'http://evochain.url/bulbusaur' } }
] as SpeciesApiResponse[];
