import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pokedex-mobile',
  webDir: '../../dist/apps/pokedex-mobile',
  bundledWebRuntime: false,
};

export default config;
